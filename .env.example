# .env приложением НЕ читается (не в assets). Список имён ключей и шаблон
# значений — в env.defines.json.example. Запуск и сборка:
#   flutter run   --dart-define-from-file=env.defines.json
#   flutter build apk --dart-define-from-file=env.defines.json
