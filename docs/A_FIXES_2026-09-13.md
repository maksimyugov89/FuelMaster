# Исправление критических («А») пунктов аудита — 13.09.2026

Основание: `docs/AUDIT_2026-09-13.md`. Каждый пункт — отдельный коммит,
проверен `flutter analyze` (0 error) и `flutter test` (14/14).

## Что уже сделано в коде

| Пункт | Коммит | Суть |
|---|---|---|
| A-9 | `5e32280` | Снят остановленный rebase (`UU AndroidManifest.xml`), рабочее дерево зафиксировано; бэкап `C:\Users\Despa\fuelmaster-backup-20260913.tgz` |
| A-1 | `1a692c0` | `.env` убран из `assets`; в release ключи читаются только из `--dart-define`; AI-вызовы идут через собственный прокси; добавлен серверный прокси `server/ai_proxy/` |
| A-3 | `9bf9b16` | `android/keystore.properties` убран из индекса git (файл на диске остался) |
| A-4 | см. коммит A-4/A-5 | Убран дубль `ALTER TABLE ... weather_multiplier` (v7/v8), все миграции идемпотентны (`PRAGMA table_info`), ошибка миграции больше не блокирует запуск |
| A-5 | см. коммит A-4/A-5 | Тесты БД на реальном SQLite (`sqflite_common_ffi`) + 3 теста миграций, включая регрессию на дубль ALTER |
| A-2 | см. коммит A-2 | Настоящие `firestore.rules` (владелец `uid`, deny-by-default), индексы вынесены в `firestore.indexes.json` |
| A-6 | см. коммит A-6/A-7/A-8 | App Check включается в `main()` при старте (debug — debug-провайдер), из экрана регистрации убран |
| A-7 | см. коммит A-6/A-7/A-8 | Единый `PremiumService`: один `productId`, один подписчик `purchaseStream`, восстановление покупок при старте |
| A-8 | см. коммит A-6/A-7/A-8 | Версия APK берётся из `pubspec.yaml` (`flutter.versionCode/Name`) вместо зашитых 1/1.0.0 |

## Что может сделать только владелец (внешние действия)

1. **Ротировать ключи** — считать утёкшими (старые сборки содержали `.env` в assets):
   - `DEEPSEEK_API_KEY` (OpenRouter) — проверить расходы в кабинете;
   - `GEOAPIFY_API_KEY` — ограничить по appId/HTTP-referer;
   - `WEATHER_API_KEY` (WeatherAPI);
   - ключ Yandex Maps из истории git (`AndroidManifest.xml`) — обновить в кабинете,
     старый отозвать;
   - пароль keystore — сменить и, при публикации APK, сменить upload key
     через Play App Signing.
2. **Развернуть AI-прокси** — `server/ai_proxy/README.md` (systemd + nginx),
   затем прописать `AI_PROXY_URL` в `env.defines.json` и убрать оттуда
   `DEEPSEEK_API_KEY` для release.
3. **Задеплоить правила** — `firebase deploy --only firestore:rules,firestore:indexes`
   (проект `fuelmaster-984fb`), после чего включить App Check enforcement.
4. **Решить по `applicationId`** — `com.example.fuelmaster` (см. комментарий в
   `android/app/build.gradle.kts`): смена = новое приложение, требуется
   обновить ключ Yandex Maps и `google-services.json`.

## Проверки, выполненные по факту

- Анонимное чтение `users` в проекте `fuelmaster-984fb` → HTTP 403
  `PERMISSION_DENIED`: база не открыта наружу.
- Дубль `ALTER TABLE` подтверждён как фатальный: SQLite возвращает
  `duplicate column name`.
- `flutter analyze`: 0 error, 108 замечаний (было 111).
- `flutter test`: 14/14 (было 11, добавлены 3 теста миграций).
