# B-пункты аудита: что сделано (13.09.2026)

Основание: `docs/AUDIT_2026-09-13.md`, раздел B. Коммиты по порядку.

| Пункт | Что сделано | Коммит |
|---|---|---|
| B-1 | `syncCarsWithFirestore` больше не удаляет локальные авто, которых нет в облаке (лог + отправка наверх) | 6da4ccd |
| B-2 | полный синк авто вынесен в syncWithFirestore() и дебаунсится 10 минут | 6da4ccd |
| B-3 | убран повторный вызов AI при пустом ответе, из логов убраны тела ответов | 42e8739 |
| B-4 | таймауты 10 c в weather и geocode, отдельная обработка таймаута | 42e8739 |
| B-5 | int→double через num вместо `as double`, регрессионный тест | 42e8739 |
| B-6 | геолокация на старте только для зарегистрированных + таймаут 15 c | f07d23d |
| B-7 | экспорт истории не удаляется до конца шаринга, уникальное имя файла | 6da4ccd |

## Остальные пункты
| B-8 | страницы собираются из провайдеров (язык/тема/авто/история не замораживаются), `Provider.of` убран из `dispose`, история — копия, а не общая ссылка | a5bfc3f |
| B-9 | единый источник ad-unit ID (`lib/utils/ad_ids.dart`), межстраничная реклама грузится заранее и показывается только загруженной | a5bfc3f |
| B-10 | профиль пишется с 3 ретраями; при провале — переход в режим входа и досоздание профиля при входе и на старте | 84cff2d |
| B-11 | источник правды о сессии — `FirebaseAuth.authStateChanges()`; выход чистит локальные авто/историю и сбрасывает премиум | 84cff2d |
| B-12 | удаление аккаунта (Firestore + Auth + локальная БД) с подтверждением; страницы политики конфиденциальности и условий | 84cff2d |
| B-13 | 48 предупреждений `use_build_context_synchronously` → 0; правило включено как ошибка в `analysis_options.yaml` | 33b7f92 |

Раздел B закрыт целиком. Итог: `flutter analyze` — 0 ошибок (59 замечаний, было 106), `flutter test` — 16/16.

Осталось за пользователем: опубликовать текст политики по публичному URL (нужен для карточки Google Play) и задеплоить правила Firestore.

## Внешние блокеры (не код)
- ~~Правила Firestore задеплоить не удалось~~ — СНЯТО 2026-09-13: права выданы
  (maksim.yugov.89@gmail.com — Owner проекта fuelmaster-984fb), правила в проде
  совпадают с репозиторием (ruleset `2026-09-13T09:41:33Z`), анонимное чтение
  `users` → 403.
- Хвост B-1 (машины без номера схлопывались в одну запись) закрыт кодом —
  см. `docs/TAILS_2026-09-13.md`.
- AI-прокси (`server/ai_proxy/`) требует хост и свежий ключ провайдера в серверном env (ключ в APK больше не попадает).
- Публичная история очищена (`git filter-repo` + force-push: keystore.properties, ключ Yandex Maps, crash_log.txt), но GitHub продолжает отдавать файл по старому SHA из кэша — нужен запрос в поддержку GitHub; пароль keystore и API-ключи всё равно подлежат ротации.